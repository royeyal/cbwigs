{
  "mcpServers": {
    "webflow-cbwigs": {
      "command": "npx",
      "args": ["-y", "webflow-mcp-server@latest"],
      "env": {
        "WEBFLOW_TOKEN": "${env:WEBFLOW_TOKEN}"
      }
    }
  }
}
